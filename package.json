{
  "name": "@technative/rank",
  "version": "1.1.0",
  "description": "A ranking algorithm for tournament data",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "prepare": "npm run build",
    "prepublishOnly": "npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "eslint"
  },
  "files": [
    "lib/**/*"
  ],
  "keywords": [
    "sort"
  ],
  "author": "TechnativeLive",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@types/lodash.get": "^4.4.6",
    "eslint": "^7.22.0",
    "prettier": "^2.2.1",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "lodash.get": "^4.4.2"
  }
}
